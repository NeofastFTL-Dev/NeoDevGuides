# Server Installation

1. Download the server files from CurseForge
2. Extract into an empty folder
3. Run `start.bat` or `run.sh`
4. Accept the EULA (eula.txt → true)
5. Start server again

Ports:
Default Minecraft port: 25565

Forward the port in your router to allow friends to join.
